#!/bin/sh
# run this from the Contents/MacOS/ directory to add o2host.sh to the
# application

cp ../../../../o2host.sh .
# confirm that we did it by printing the final full file path:
ls -l o2host.sh
